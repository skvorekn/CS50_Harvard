#include <cs50.h>
#include <stdio.h>

int main(void)
{

    /* get a string from a user and store it in a string variable */
    string name = get_string("What is your name?\n:")

    printf("hello, name\n");
}

/* clang -o hello hello.c */
/* ./hello */
/* make hello */
/* gives more configuration options */
/* make is not a compiler, it is just a program that uses clang in an automated way */

/* error hello.c:line number:character: error:

compile it first:
make hello
./hello

*/